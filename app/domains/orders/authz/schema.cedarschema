namespace Mixology {
    entity Actor enum ["owner", "manager", "sommelier", "bartender", "anonymous"];
    entity Menu;

    entity Order {
        MenuID: Menu,
        Status: String
    } tags String;
}

namespace Mixology::Order {
    action list, get, place, complete, cancel, tag, untag appliesTo {
        principal: Mixology::Actor,
        resource: Mixology::Order,
        context: {}
    };
}
